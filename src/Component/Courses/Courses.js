import React from 'react';
import './Courses.css' ;
const Courses = (props) => {
    const {name , price , img} = props.service ;
    return (
        <div>
           
        </div>
    );
};

export default Courses;